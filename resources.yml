resources:
  - name: nginx-image
    type: image
    integration: avinci
    source:
      name: "library/nginx"
      isPull: false
      tag: latest

  - name: demoCluster
    type: ecsCluster
    integration: aws-demo
    source:
      name : "default" #name of the cluster we are deploying
      region: "us-east-1"
      