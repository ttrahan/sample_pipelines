resources:
  - name: nginx-image
    type: image
    integration: avinci-dh
    source:
      name: "library/nginx"
      isPull: false
      tag: latest

  - name: demoCluster
    type: ecsCluster
    integration: ecsCluster
    source:
      name : "prod-aws" #name of the cluster we are deploying
      region: "us-east-1"
